Project: Caesar_cipher

This program in C is a basic encryption mechanism, taking a user string input and outputting an encoded output. It does so by shifting 
the input characters a predetermined amount in the dictionary to correspond to a new character. 